package africa.semicolon;

import africa.semicolon.chapterThree.Bike;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class BikeTest2 {
    @Test
    public void testThatAccelerationCanBeSet(){
        // given
        Bike2 okada = new Bike ();
        // when
        okada.setMaxAccelerationInKmPerHour(20);
        // confirm
        assertEquals(20, okada.getMaxAccelerationInKmPerHour());
        public boolean IsOn
                if(isOn)

                    this.IsOn=IsOn;
                if (this.isOn) {
                    current GearNumber 1;
                }
                if (!this.isOn)
                    current GearNumber=0;
                }
    }
}
